import { AddressSelectionModel } from "../address/address-selection.model";
import { CustomerModel } from "./customer.model";

export interface CustomerSelectBoxModel extends CustomerModel {
    id: number;
}